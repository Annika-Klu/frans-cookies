export type Cookie = {
  name: string;
  price: number;
  description?: string;
  image?: any;
};

export type CookieViewModel = {
  name: string;
  price: number;
  description?: string;
  image: string;
};